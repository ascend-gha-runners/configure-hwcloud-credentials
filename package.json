{
  "name": "configure-hwcloud-credentials",
  "version": "1.0.0",
  "description": "GitHub Action to configure Huawei Cloud OBS credentials using GitHub OIDC",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt",
    "lint": "eslint src/*.js",
    "test": "jest"
  },
  "keywords": [
    "github-actions",
    "huawei-cloud",
    "obs",
    "credentials",
    "oidc"
  ],
  "author": "Your Name",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0",
    "eslint": "^8.56.0",
    "jest": "^29.7.0"
  }
}